package sept.ex_16092024;

public class Lab032_qna {
    public static void main(String[] args) {
        // int enum=10;
        //System.out.println(enum);
        //'enum' is a keyword,
        // and may not be used as an identifier/variable
        int Enum=10;
        System.out.println(Enum);
    }
}
